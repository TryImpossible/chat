const config = {
  // 启动端口
  port: 3001,
  // 数据库配置
  database: {
    DATABASE: 'chat',
    USERNAME: 'root',
    PASSWORD: 'password',
    PROT: '3306',
    HOST: 'localhost',
  },
};

module.exports = config;
